// Fill in your secure credentials in server targets.
// Add and remove servers as needed.
// If more than one project uses this file it's convenient
// to put one instance of this file at one place and link
// to it from inside project folders.
//
// servername, as in .ssh/config f.e.
{
  "servername": {
    "host": "some.host.com",
    "username": "username",
    "privateKey": "/full/path/to/private/sshkeyfile",
    "passphrase": "keypassword-or-sshagentpassword"
  }
}
